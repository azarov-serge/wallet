import { ITheme } from '@/components/themes/types';

import { typography } from '@/components/themes/constants/typography';
import { palette } from './palette';

export const dark: ITheme = {
    palette,
    typography,
};
